# Pro Workflow Template

This is a reference template. Run `/setup` to generate a tailored version for your project.

## Stack
[Your tech stack -- detected by /setup]

## Code Style
- Prettier handles formatting automatically (hook enforced)
- No console.log in production code
- Prefer simple, readable code over clever abstractions

## Workflow
- Use Plan Mode (Shift+Tab) before changes touching 3+ files
- Use @filename references instead of pasting code
- Verify changes with your test command after edits
- Use /compact when conversation slows down
- Use /btw for quick side questions

## Build & Test Commands
- Build: [your build command]
- Test: [your test command]
- Lint: [your lint command]
- Dev: [your dev command]
